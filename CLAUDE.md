## Git Rules

### ブランチ

- `main` から切る
- 命名: `<type>/<短い説明>` 例: `feat/search-empty-state`
- 1ブランチ = 1つの機能・修正。長く育てず、こまめに main に戻す

### コミットメッセージ

形式: `<emoji> <type>: <日本語で50字以内>`

| emoji | type       | 用途               |
| ----- | ---------- | ------------------ |
| ✨    | `feat`     | 新しい機能         |
| 🐛    | `fix`      | バグ修正           |
| 💄    | `ui`       | 見た目・UX の調整  |
| ♻️    | `refactor` | 動作を変えない整理 |
| ✅    | `test`     | テストの追加・修正 |
| 📝    | `docs`     | ドキュメントの更新 |
| 🔧    | `chore`    | 設定まわり         |

絵文字と type は必ずセットで使う。表にあるもの以外は使わない。

例:

- `✨ feat: 検索結果が0件のときにメッセージを表示する`
- `🐛 fix: 日付が1日ずれて保存される問題を修正`
- `💄 ui: 送信ボタンの位置を入力欄の下に移動`
- `♻️ refactor: 検索処理を関数に切り出す`
- `✅ test: 空文字入力時のバリデーションのテストを追加`
- `📝 docs: README にトラブルシューティングを追加`
- `🔧 chore: ESLint の設定を追加`

### コミットの粒度

- 1コミット = 1つの意味のある変更
- 「あとで説明できる単位」で切る。機能追加とリファクタを混ぜない
- 変更が複数の意味を持つ場合は分割する
